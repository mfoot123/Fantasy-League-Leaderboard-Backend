[project]
name = "fantasy-league-leaderboard-backend"
version = "0.1.0"
description = "Fantasy League leaderboard backend API"
requires-python = ">=3.11"
dependencies = [
    "Flask==3.1.1",
    "Flask-Cors==5.0.0",
    "requests==2.32.3",
]

[tool.vercel]
entrypoint = "index:app"
